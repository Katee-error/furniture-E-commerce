import { extendTheme } from "@chakra-ui/react";


const Color = {
    primary : '#0a1d37',
    blue: '#d6e5fb',
    heading : '#0a1d37',
    text: '#999',
}
const theme = extendTheme({
    Color,

    })

    export default theme
